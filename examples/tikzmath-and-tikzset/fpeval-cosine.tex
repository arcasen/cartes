\documentclass{ctexart}
\usepackage{tikz}
\usepackage{xfp}
\usetikzlibrary{math}

\begin{document}


\tikzmath{
  \myangle = 60; % TikZ 默认这是 90 度
  \s1 = sin(\myangle); % TikZ 结果是 1
}

\s1

% 此时如果在 \fpeval 中直接用 \myangle
\fpeval{sind(\myangle)} 实际上计算的是 sin(90 rad) ≈ 0.8939
\end{document}