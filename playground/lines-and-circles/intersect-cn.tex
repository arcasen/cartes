\documentclass{article}
\usepackage{tikz}
\usepackage{tikz-3dplot}

\usetikzlibrary{cartes}
% \input{../../tikzlibraryvc.code.tex}

\begin{document}

\begin{tikzpicture}
  \tikzmath{
    \radius = 1;
    \A{1} = 1; \A{2} = 1;
    % \B{1} = 0; \B{2} = 2;
    % \C{1} = 2; \C{2} = 0;
  }
  \tikzset{
    plane/intersect cn={\A,\radius,1,2,-2,\D,\E},
  }
  \axes[grid] (-4:4,-4:4);
  \coordinate (A) at (\A{1},\A{2});
  % \coordinate (B) at (\B{1},\B{2});
  % \coordinate (C) at (\C{1},\C{2});
  \coordinate (D) at (\D{1},\D{2});
  \coordinate (E) at (\E{1},\E{2});
  \draw[thick,red] (A) circle (\radius);
  \draw[thick] (D) -- (E);
  \foreach \p/\placement in {A/left,%B/right,C/above left,
  D/below right,E/left}{
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

\begin{tikzpicture}
  \tikzmath{
    \radius = 3;
    \A{1} = 1; \A{2} = 1;
    % \B{1} = 0; \B{2} = 2;
    % \C{1} = 2; \C{2} = 0;
    \l{1,1} = -1; \l{2,1} = 2; \l{3,1} = -2;
  }
  \tikzset{
    % plane/intersect cn={\A,\radius,-1,2,-2,\D,\E},
    plane/intersect cn={\A,\radius,\l{1,1},\l{2,1},\l{3,1},\D,\E},
  }
  \axes[grid] (-4:4,-4:4);
  \coordinate (A) at (\A{1},\A{2});
  % \coordinate (B) at (\B{1},\B{2});
  % \coordinate (C) at (\C{1},\C{2});
  \coordinate (D) at (\D{1},\D{2});
  \coordinate (E) at (\E{1},\E{2});

  \draw[thick,red] (A) circle (\radius);
  % \draw[thick] (D) -- (E);
  \segment[draw=teal,clip=(-2:4,-1:5)] (\l);% 绘制直线

  \foreach \p/\placement in {A/left,%B/right,C/above left,
  D/below right,E/left}{
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

\end{document}