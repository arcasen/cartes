\documentclass{article}
\usepackage{tikz}
\usepackage{tikz-3dplot}
% \Asetikzlibrary{cartes}
\input{../../tikzlibraryvc.code.tex}

\begin{document}

2D

\begin{tikzpicture}
  \tikzmath{
    \A{1} = 1;
    \A{2} = 1;
    \B{1} = 2;
    \B{2} = 0;
  }

  \tikzset{
    plane/length={\A,\B,\length},
  }

  % 绘制坐标轴
  \draw[help lines,teal,-latex] (0,0) -- (1,0) node[right]{$x$};
  \draw[help lines,teal,-latex] (0,0) -- (0,1) node[below left]{$y$};

  \coordinate (A) at (\A{1},\A{2});
  \coordinate (B) at (\B{1},\B{2});

  \draw [thick,-latex] (A) -- (B);

  \foreach \p/\placement in {A/below,B/below} {
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

3D

\begin{tikzpicture}[
  x={({0.5*cos(-135)},{0.5*sin(-135)})}, % default: cm
  y={(1cm,0cm)}, 
  z={(0cm,1cm)}
]
  \tikzmath{
    \A{1} = 1;
    \A{2} = 1;
    \A{3} = 0;
    \B{1} = 2;
    \B{2} = 0;
    \B{3} = 2;
  }

  \tikzset{
    space/length={\A,\B,\length},
  }

  % 绘制坐标轴
  \draw[help lines,teal,-latex] (0,0,0) -- (1,0,0) node[right]{$x$};
  \draw[help lines,teal,-latex] (0,0,0) -- (0,1,0) node[below left]{$y$};
  \draw[help lines,teal,-latex] (0,0,0) -- (0,0,1) node[above]{$z$};

  \coordinate (A) at (\A{1},\A{2},\A{3});
  \coordinate (B) at (\B{1},\B{2},\B{3});

  \draw [thick,-latex] (A) -- (B);
  
  \foreach \p/\placement in {A/below,B/below} {
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

\end{document}