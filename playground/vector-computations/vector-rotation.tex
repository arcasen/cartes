\documentclass{article}
\usepackage{tikz}
% \usetikzlibrary{cartes}
\input{../../tikzlibraryvc.code.tex}

\begin{document}

2D

\begin{tikzpicture}
  \tikzmath{
    \A{1} = 1;
    \A{2} = 1;
    \B{1} = 3;
    \B{2} = 1;
    \C{1} = 4;
    \C{2} = 4;
  }

  \tikzset{
    plane/angle={\A,\B,\C,\a},
    plane/rotate={\A,\B,-\a,\D},
  }
  \draw (0,0) node[below] {\a};

  \coordinate (A) at (\A{1},\A{2});
  \coordinate (B) at (\B{1},\B{2});
  \coordinate (C) at (\C{1},\C{2});
  \coordinate (D) at (\D{1},\D{2});

  \foreach \p/\placement in {A/below,B/below,C/above,D/above} {
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

3D

\begin{tikzpicture}
  \tikzmath{
    % 旋转轴
    \A{1} = 1;
    \A{2} = 1;
    \A{3} = 1;
    % 待旋转向量
    \B{1} = 1;
    \B{2} = 2;
    \B{3} = 3;
  }

  \tikzset{
    space/normalize/origin={\A,\u},
    space/rotate/origin={\u,\B,60,\C}, % 2,1,3
  }

  % 绘制坐标轴
  \draw[help lines,teal,-latex] (0,0,0) -- (1,0,0) node[right]{$x$};
  \draw[help lines,teal,-latex] (0,0,0) -- (0,1,0) node[below left]{$y$};
  \draw[help lines,teal,-latex] (0,0,0) -- (0,0,1) node[above]{$z$};

  \coordinate (A) at (\A{1},\A{2},\A{3});
  \coordinate (B) at (\B{1},\B{2},\B{3});
  \coordinate (C) at (\C{1},\C{2},\C{3});

  \foreach \p/\placement in {A/below,B/below,C/above} {
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

\begin{tikzpicture}
  \tikzmath{
    % 旋转轴
    \A{1} = 3;
    \A{2} = 4;
    \A{3} = 0;
    % 待旋转向量
    \B{1} = 2;
    \B{2} = -1;
    \B{3} = 2;
  }

  \tikzset{
    space/normalize/origin={\A,\u},
    space/rotate/origin={\u,\B,180,\C}, % -1.52, 1.64, -2
  }

  % 绘制坐标轴
  \draw[help lines,teal,-latex] (0,0,0) -- (1,0,0) node[right]{$x$};
  \draw[help lines,teal,-latex] (0,0,0) -- (0,1,0) node[below left]{$y$};
  \draw[help lines,teal,-latex] (0,0,0) -- (0,0,1) node[above]{$z$};

  \coordinate (A) at (\A{1},\A{2},\A{3});
  \coordinate (B) at (\B{1},\B{2},\B{3});
  \coordinate (C) at (\C{1},\C{2},\C{3});

  \foreach \p/\placement in {A/below,B/below,C/above} {
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

\end{document}