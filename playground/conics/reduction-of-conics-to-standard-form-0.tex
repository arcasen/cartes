\documentclass{standalone}
\usepackage{tikz}

\usetikzlibrary{cartes}

\begin{document}

\begin{tikzpicture}
  \tikzmath{
    \coefs{1} = 1/9; \coefs{2} = 0; \coefs{3} = 1/1; 
    \coefs{4} = 0; \coefs{5} = 0; \coefs{6} = -1;
    \A{1,1} = 3;  \A{2,1} = 0; \A{3,1} = 1;
  }
  \tikzset{
    % x' C x = 0 (x' = transpose(x))
    % x = R y, R 是逆旋转矩阵
    % 代入
    % y' R' C R y = 0
    conics/define/quadratic={\coefs,\CO},
    conics/rotation matrix={-30,\R},
    mat/stdout={\R,3,3},
    mat/congruence={\CO,\R,3,\CR},
    mat/stdout={\CR,3,3},
    conics/translation matrix={-2,-3,\T},
    mat/stdout={\T,3,3},
    mat/congruence={\CO,\T,3,\CT},
    mat/stdout={\CT,3,3},
    % mat/product={\R,\A,3,3,1,\B},
    % mat/product={\T,\A,3,3,1,\C},
    mat/solve={\R,\A,3,1,\B},
    mat/solve={\T,\A,3,1,\C},
  }

  \coordinate (A) at (\A{1,1},\A{2,1});
  \coordinate (B) at (\B{1,1},\B{2,1});
  \coordinate (C) at (\C{1,1},\C{2,1});

  \axes[grid] (-5:5,-5:5);
  \conic[draw=teal,thick] (\CO);
  \conic[draw=red,thick] (\CR);
  \conic[draw=purple,thick] (\CT);
  
  \foreach \p/\placement in {A/above, B/above, C/above}{
    \fill[red] (\p) circle (2pt);
    \draw (\p) node[\placement] {$\p$};
  }
\end{tikzpicture}

\end{document}  
