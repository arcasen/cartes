\begin{tikzpicture}
  \draw[help lines] (-2,0) grid (2,4);
  
  \tikzmath{
    \a = 0.5; 
    for \x in {-2, -1, ..., 2}{
      \y = \a * (\x)^2;  % 注意: 不要写成 \y = \a * \x^2; 
      {\fill (\x, \y) circle (2pt) node[below] {$y|_{\x}$};};
    };
  }
\end{tikzpicture}